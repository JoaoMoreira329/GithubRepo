public class Main {

    public static void main(String[] args) {

        //Creates an instance of the Paint class with specified values for columns, rows and a delay
        Paint paint = new Paint(40, 40, 200);

        //call for the init method, initializes the painting area and user interaction
        paint.init();
    }
}
